<form method="post" action="{{ path('app_department_delete', {'DEPTNO': department.DEPTNO}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ department.DEPTNO) }}">
    <button class="btn">Delete</button>
</form>
