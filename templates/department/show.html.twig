{% extends  is_granted('ROLE_ADMIN')? '/admin_home/index.html.twig' : 'base.html.twig' %}

{% block title %}Department{% endblock %}

{% block body %}
    <h1>Department</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>DEPTNO</th>
                <td>{{ department.DEPTNO }}</td>
            </tr>
            <tr>
                <th>DNAME</th>
                <td>{{ department.DNAME }}</td>
            </tr>
            <tr>
                <th>LOC</th>
                <td>{{ department.LOC }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_department_index') }}">back to list</a>

    <a href="{{ path('app_department_edit', {'DEPTNO': department.DEPTNO}) }}">edit</a>

    {{ include('department/_delete_form.html.twig') }}
{% endblock %}
